{% extends 'site/templates/_layout.twig' %}

{% block main %}
  <h1>{{ page.title }}</h1>
  {% for conference in conferences %}
    <div class="hero">
      {% include 'includes/conference-details.html.twig' %}
    </div>
  {% endfor %}
{% endblock %}
