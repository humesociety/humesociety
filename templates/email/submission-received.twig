{% extends 'email/base.twig' %}

{% block image %}{{ image }}{% endblock %}

{% set conference = submission.conference %}

{% block content %}
  <p>A new paper has just been submitted for the {{ conference }}. {% if conference.submissions|length == 1 %}There is now 1 submission in the database.{% else %}There are now {{ conference.submissions|length }} submissions in the database.{% endif %}</p>
  <p><strong>Title:</strong> {{ submission.title }}</p>
  <p><strong>Keywords:</strong> {{ submission.keywords }}</p>
  <p><strong>Abstract:</strong> {{ submission.abstract }}</p>
{% endblock %}
