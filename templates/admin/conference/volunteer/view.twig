{% extends 'admin/conference/_layout.twig' %}

{% block title %}Conference Volunteers{% endblock %}

{% block main %}
  {% embed 'admin/_embed/box.twig' %}
    {% block title %}Conference Volunteers{% endblock %}
    {% block body %}
      <p>Users of this web site have the option to indicate their availability to act as reviewers, commentators, or chairs. In inviting people to perform these roles, you are not limited to those who have actively volunteered, but these will presumably be a good place to start. When indicating their availability, users are also required to submit a comma-separated list of keywords representing their areas of expertise.</p>
      <nav class="tabs">
        <a data-tab="review"{% if tab == 'review' %} class="is-active"{% endif %}>Reviewers</a>
        <a data-tab="comment"{% if tab == 'comment' %} class="is-active"{% endif %}>Commentators</a>
        <a data-tab="chair"{% if tab == 'chair' %} class="is-active"{% endif %}>Chairs</a>
      </nav>
      <div class="tab-pane{% if tab == 'review' %} is-active{% endif %}" id="review">
        {% include 'admin/_include/volunteer/reviewer-list.twig' %}
      </div>
      <div class="tab-pane{% if tab == 'comment' %} is-active{% endif %}" id="comment">
        {% include 'admin/_include/volunteer/commentator-list.twig' %}
      </div>
      <div class="tab-pane{% if tab == 'chair' %} is-active{% endif %}" id="chair">
        {% include 'admin/_include/volunteer/chair-list.twig' %}
      </div>
    {% endblock %}
  {% endembed %}
{% endblock %}
