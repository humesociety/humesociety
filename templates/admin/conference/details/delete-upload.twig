{% extends 'admin/conference/_layout.twig' %}

{% block title %}Delete File{% endblock %}

{% block main %}
  <div class="box content">
    <div class="heading">
      <h5>{{ conference.ordinal }} Hume Conference: Delete {{ filename }}</h5>
      <div class="heading-right">
        <div class="input">
          <a href="{{ path('admin_conference_details_edit', { tab: 'files' }) }}" class="button"><i class="fas fa-chevron-left"></i> Back to Conference Details</a>
        </div>
      </div>
    </div>
    <p>Are you sure you want to delete this file? This action cannot be undone!</p>
    {{ form_start(uploadForm) }}
      {{ form_row(uploadForm._token) }}
      <div class="buttons">
        <button type="submit">Delete</button>
      </div>
    {{ form_end(uploadForm, { render_rest: false }) }}
  </div>
{% endblock %}
